package auth_test

import "testing"

func TestAuthIntegrationHarnessIsReady(t *testing.T) {
	t.Skip("auth routes and session service are not merged in this branch yet; keep this harness as the landing zone for the real auth integration tests")
}
